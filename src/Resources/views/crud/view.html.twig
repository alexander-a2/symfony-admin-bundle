{% extends '@Admin/layout/dashboard.html.twig' %}

{% block main %}
    <div class="row mb-2">
        <div class="col-md-6">
            <a href="javascript:history.back()"
               class="btn btn-secondary">&larr; {{ 'Back'|trans }}</a>
            <a href="{{ path('admin_crud_edit', {'entityClassName': entityClassName, 'entityId': entityId}) }}"
               class="btn btn-primary">{{ 'Edit'|trans }}</a>
        </div>
        <div class="col-md-6 text-right">
            <a href="{{ path('admin_crud_delete', {'entityClassName': entityClassName, 'entityId': entityId}) }}"
               class="btn btn-danger" onClick="if(!confirm('Are you sure?')){return false;}">{{ 'Delete'|translate }}</a>
        </div>
    </div>
    <table class="table table-bordered table-hover table-striped">
        {% for item in data %}
            <tr>
                <td width="200"><b>{{ item.name|trans }}</b></td>
                <td>{{ item.value }}</td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}