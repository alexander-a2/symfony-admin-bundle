{% extends '@Admin/layout/dashboard.html.twig' %}

{% block main %}
    <div class="row mb-2">
        <div class="col-md-6">
            <a href="{{ path('admin_crud_edit', {'entityClassName': entityClassName}) }}"
               class="btn btn-success">{{ 'admin.controls.add'|trans }}</a>
        </div>
        <div class="col-md-6 text-right">
        </div>
    </div>
    {{ datasheet(entityDatasheet) }}
{% endblock %}